(ns aocYY.dayDD
  (:require
   [clojure.java.io :as io]
   [clojure.pprint :as pp]
   [clojure.set :as set]))

(defn part-1
  "TBD"
  [file-name]
  ;(->> file-name
  ;     rh/read-lines
  ;     pp/pprint)
  1)

(defn part-2
  "TBD"
  [file-name]
  2)

